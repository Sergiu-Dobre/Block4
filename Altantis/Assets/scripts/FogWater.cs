using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FogWater : MonoBehaviour
{ /*
public float waterH;
private bool isUnderWater;
private Color normal;
private Color underwaterC;


    // Start is called before the first frame update
    void Start()
    {
        normal = new Color(0.05f, 0.05f, 0.05f, 0.05f);
        underwaterC = new Color(0.022f, 0.065f, 0.77f, 0.05f);
    }

    // Update is called once per frame
    void Update()
    {
        if ((transform.position.y < waterH) != isUnderWater)
        {
            isUnderWater = transform.position.y < waterH;
            if (isUnderWater)
            
                SetUnderWater();
            
            if (!isUnderWater)
            
                SetNormal();
            
        }
    }


    void SetNormal(){
        RenderSettings.fogColor = normal;
        RenderSettings.fogDensity = 0.01f;
    }
    void SetUnderWater(){
        RenderSettings.fogColor = underwaterC;
        RenderSettings.fogDensity = 0.1f;
    }*/
}
